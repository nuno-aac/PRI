extends layout

block content
    .w3-card-4
        header.w3-container.w3-light-green
            h2.w3-padding-16 Lista de Alunos
                a(href="alunos/registar") 
                    button.w3-card.w3-button.w3-white.w3-right.w3-text-black +
                    
        if lista.length > 0
            .w3-container.w3-margin
                table.w3-table-all
                    tr
                        th Num
                        th Nome
                        th Git
                        th Opções
                    each a in lista
                        tr
                            td
                                a=a.Número
                            td
                                a(href=`alunos/${a.Número}`)= a.Nome
                            td
                                a= a.Git
                            td
                                .w3-container.w3-flexrow.w3-right
                                    a(href=`alunos/editar/${a.Número}`)
                                        button.w3-button.w3-light-green.w3-right.w3-text-black.w3-margin-right Editar
                                    form(action=`/alunos/${a.Número}` method='POST')
                                        input(type="hidden" id="delete" name="delete" value=1)
                                        input(type="hidden" id="id" name="id" value=a._id)
                                        input.w3-button.w3-light-green.w3-right.w3-text-black.w3-margin-right(type='submit' value='Remover')
        else 
            .w3-container
                p ainda não há alunos
        footer.w3-container.w3-light-green.w3-padding-16.w3-center
            adress Gerado por Nuno Cunha | PRI2020 with 💚